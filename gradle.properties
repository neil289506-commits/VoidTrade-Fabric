# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Fabric Properties
# IMPORTANT: verify these against https://fabricmc.net/develop before building —
# loader/API versions update frequently and I can't guarantee these are the
# exact latest at the time you build.
minecraft_version=26.1.2
loader_version=0.16.10

# Mod Properties
mod_version=1.0.0
maven_group=com.neil
archives_base_name=voidtrade

# Dependencies
# Fabric API version below found via Modrinth search at the time this project was assembled.
# Re-check https://modrinth.com/mod/fabric-api before building — this may be outdated.
fabric_version=0.155.2+26.1.2
